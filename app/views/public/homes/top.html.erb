<div>
  <%= flash[:notice] %>
</div>

<main>
    <p id="notice"></p>
  <div class="container px-5 px-sm-0">
    <div class="row">
      <div class="col-md-3">
        <table class="table table-bordered border-dark">
          <thead>
            <tr><th scope="col">ジャンル検索</th></tr>
          </thead>

          <tbody class="table-borderless">
            <% @genres.each do |genre| %>
              <tr>
              <td><%= genre.name %></td>
            <% end %>
          </tbody>
        </table>
      </div>

      <div class="main-visual col-md-8 offset-md-1">
        <div class="card" style="max-width: 400px;">
          <%= image_tag 'default-image.jpg', size: "700x300" %>
            <div class="card-img-overlay ">
              <p class="navbar-white bg-white text-black"><br>
                ようこそ、ながのCAKEへ！<br>
                このサイトは、ケーキ販売のECサイトになります。<br>
                会員でない方も商品の閲覧はできますが、<br>
                購入には会員登録が必要となります。<br>
                <br>
              </p>
            </div>
        </div>
        <br>

        <div class="col-md-9">
          <h4 class="font-weight-bold">新着商品</h4>
        <div class="main-visual col-md-8 offset-md-1">
           <div class="item-wrap">
              <% @items.each do |item| %>
              <div class="item-box font-weight-bold">
                <%= link_to item_path(item.id) do %>
                  <%= image_tag item.image , size: "200x100"%>
                <% end %>
                <div class="item-comment">
                  <span class="item-body"><%= item.name %></span><br>
                  <span class="item-title">¥<%= item.price.to_s(:delimited) %></span>
                </div>
              </div>
            <% end %>


        <div class="text-right">
          <%= link_to "全ての商品を見る >", items_path %>
        </div>
      </div>
    </div>
  </div>
</main>